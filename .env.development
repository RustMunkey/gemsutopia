# ========================================
# LOCAL DEVELOPMENT ENVIRONMENT
# ========================================
# This file is for local Docker-based development.
# Copy relevant values to .env.local to use local services.
# Redis is intentionally not configured - the app works
# without it (no caching, all queries hit DB directly).
# ========================================

# Local PostgreSQL (Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gemsutopia

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NODE_ENV=development

# Auth (generate your own secrets)
BETTER_AUTH_SECRET=local-dev-secret-change-in-production
JWT_SECRET=local-dev-jwt-secret-change-in-production
ADMIN_JWT_SECRET=local-dev-admin-jwt-secret-change-in-production

# Redis - NOT SET (app works without caching in dev)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Pusher - NOT SET (falls back to polling in dev)
# PUSHER_APP_ID=
# PUSHER_KEY=
# PUSHER_SECRET=
# PUSHER_CLUSTER=

# Payments - Use test/sandbox keys
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# PAYPAL_CLIENT_ID=sandbox-client-id
# PAYPAL_MODE=sandbox

# Email - Optional in dev (contact form will error without it)
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=
